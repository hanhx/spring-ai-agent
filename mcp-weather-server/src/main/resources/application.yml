server:
  port: 9082

spring:
  ai:
    mcp:
      server:
        name: weather-mcp-server
        version: 1.0.0
        type: SYNC
        protocol: SSE

logging:
  level:
    com.example.mcp: DEBUG
    io.modelcontextprotocol: DEBUG
